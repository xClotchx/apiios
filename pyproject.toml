[project]
name = "apiios"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.vercel]
entrypoint = "api.download:app"

[tool.vercel.functions]
"api/download.py" = { maxDuration = 30 }